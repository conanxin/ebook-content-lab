# 路线抽取提示词

目标：从 OCR 文本中抽取《从大都到上都》书中实际记录的徒步路线。

规则：

1. 只能使用书中内容决定路线顺序、起点、终点和途经地。
2. 现代地图资料只能用于补坐标，不得用于改写路线。
3. 每一段路线必须包含页码、短摘和依据说明。
4. 短摘必须短，不复制长段落。
5. OCR 不确定、地名歧义、路线断点和坐标缺失必须写入复核 notes。
6. 书中没有明示的信息填 `null` 或 `书中未明示`。
